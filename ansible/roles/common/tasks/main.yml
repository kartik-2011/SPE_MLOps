- name: Ensure local deployment directories exist
  file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
  loop:
    - "{{ playbook_dir }}/../logs"
    - "{{ playbook_dir }}/../models/artifacts"
    - "{{ playbook_dir }}/../models/metadata"

